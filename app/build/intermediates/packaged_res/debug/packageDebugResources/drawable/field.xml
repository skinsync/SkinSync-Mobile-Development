<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:shape="rectangle">
        <shape>
            <solid android:color="@color/grey_field" />
            <!-- Isian utama dengan transparansi 100% -->
            <solid android:color="@color/grey_field" />
            <!-- Stroke pertama dengan alpha (transparansi) 100% -->
            <stroke android:width="-1dp" android:color="@color/beige" />
            <!-- Stroke kedua dengan alpha (transparansi) 20% -->
            <stroke android:width="-1dp" android:color="@color/beige" android:alpha="0.2" />
            <!-- Corner radius sebesar 5dp -->
            <corners android:radius="5dp" />
        </shape>
    </item>
</selector>